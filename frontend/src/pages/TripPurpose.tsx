/**
 * TripPurpose — "방문 목적" 페이지 (STEP3 직후, 3/3 단계).
 * Figma: 여기말GO / node 48:2817 레이아웃 기반, 백엔드 스펙에 맞게 조정.
 */
import { TripPurposeForm } from "@/features/trips/components/TripPurposeForm"

export default function TripPurpose() {
  return <TripPurposeForm />
}
