/**
 * TripConditions — STEP2 "여행 조건" 페이지.
 * Figma: 여기말GO / node 84:68 "여행 조건 온보딩 (이동 시간 x)"
 */
import { TripConditionsForm } from "@/features/trips"

export default function TripConditions() {
  return <TripConditionsForm />
}
