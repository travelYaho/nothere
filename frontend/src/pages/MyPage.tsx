/**
 * MyPage — "마이" 탭 페이지.
 * Figma: 여기말GO / node 100:2099 "Container:transform" (마이페이지)
 */
import { MyPageView } from "@/features/users/components/MyPageView"

export default function MyPage() {
  return <MyPageView />
}
