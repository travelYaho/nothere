/**
 * CustomPlace — "장소 직접 추가" 페이지.
 * Figma: 여기말GO / node 48:3842
 */
import { CustomPlaceForm } from "@/features/trips/components/CustomPlaceForm"

export default function CustomPlace() {
  return <CustomPlaceForm />
}
